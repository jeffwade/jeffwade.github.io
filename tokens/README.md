# Design Tokens

Design tokens in DTCG format. Run `npm run tokens` to generate
`src/styles/tokens.css` from `tokens/tokens.json`.

**Source of truth:** `tokens/tokens.json`

**Figma sync path (future):** Export from Figma → Style Dictionary → CSS.
Currently hand-maintained as a port of the original Plasmic token catalog.
